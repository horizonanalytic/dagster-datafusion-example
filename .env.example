# ========================================
# DataFusion + Dagster Example Configuration
# ========================================

# Environment: local, uat, or prod
ENVIRONMENT=local

# ========================================
# Local Development (LocalStack)
# ========================================

# S3 Configuration (LocalStack)
S3_BUCKET=dagster-parquet-data
S3_PREFIX=datafusion_test

# LocalStack endpoint
AWS_ENDPOINT_URL=http://localstack:4566

# Test credentials (for LocalStack)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-2

# ========================================
# UAT Environment
# ========================================

# S3 Configuration (Real S3)
# UAT_S3_BUCKET=your-uat-s3-bucket
# UAT_S3_PREFIX=dagster_uat

# AWS Credentials (use IAM role in ECS/EC2, or set explicitly)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-2

# ========================================
# Production Environment
# ========================================

# S3 Configuration (Real S3)
# PROD_S3_BUCKET=your-prod-s3-bucket
# PROD_S3_PREFIX=dagster_prod

# AWS Credentials (use IAM role in ECS/EC2)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-2

# ========================================
# Dagster Database (for docker-compose)
# ========================================

DAGSTER_POSTGRES_USER=dagster
DAGSTER_POSTGRES_PASSWORD=dagster
DAGSTER_POSTGRES_DB=dagster
DAGSTER_POSTGRES_HOST=postgres
DAGSTER_POSTGRES_PORT=5432

# ========================================
# Optional: DataFusion Service Integration
# ========================================

# If using with datafusion_service, set these:
# DAGSTER_UAT_DB_URL=postgresql://user:password@host:5432/dagster_uat
# DAGSTER_PROD_DB_URL=postgresql://user:password@host:5432/dagster_prod
# DAGSTER_UAT_ENABLED=true
# DAGSTER_PROD_ENABLED=false
# DAGSTER_SYNC_INTERVAL_SECS=300
